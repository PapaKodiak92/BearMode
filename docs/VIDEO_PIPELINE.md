# Kodiak alert video pipeline

The alert animation lives at `public/kodiak-roar.mp4`.

During alerts, the app should show that video inside the Kodiak avatar frame while the existing alert audio plays separately.
